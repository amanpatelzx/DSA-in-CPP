#include<iostream>//finding no. of digits without using loop;
#include<string>
#include<algorithm>
using namespace std;
int main(){
    string v;
    cout<<"Enter the string ";
    cin>>v;
    // sort(v.begin(),v.end());
    cout<<int(v[0]);
}
