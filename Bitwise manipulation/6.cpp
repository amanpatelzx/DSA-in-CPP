#include<iostream>//Given an integer n, flip allits bits i.e. if the given bit change it to O and vice yersa.
using namespace std;//use of XOR
int main(){
    int a=50;
    int b=~0;
    cout<<a^b;
}
